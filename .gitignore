# Python
__pycache__/
*.py[cod]
.venv/
venv/

# .env is loaded by env_bootstrap at every entry point; never commit secrets
.env
*.env
!settings.example.env
*.db
*.db-shm
*.db-wal
*.sqlite3

# audit stays internal until the team decides otherwise (external-audit/00_INDEX.md)
external-audit/

# throwaway test/run artifacts
tests/_*


# Data — never commit datasets; manifest hashes only
data/raw/
data/splits/*.parquet

# Artifacts (model binaries/metrics — never source code)
defense/models/*.ubj
defense/models/*.json
defense/models/*.pkl
/models/
*.joblib
!defense/models/artifacts/ulb_backbone.joblib
*.onnx
experiments/results/
node_modules/
dist/
build/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
__pycache__/
